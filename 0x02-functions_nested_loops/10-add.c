#include "main.h"
/**
 *add - adds two integers and returns the result
 *@x: integer input
 *@y: interger input
 *Return: returns the result of the function
 */

int add(int x, int y)
{
	return (x + y);
}
