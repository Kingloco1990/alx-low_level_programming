#include <stdio.h>
/**
 *main - execution and termination
 *
 *Return: ends main function
 */
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d", digit);
	}

	printf("\n");

	return (0);
}
